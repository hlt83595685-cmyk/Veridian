# 同步/导出流程改进 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** (1) 转换完成后一次性同步；(2) 转换失败不提交并在列表标红；(3) 仓库目录用清洗后的标题命名、身份改由 item.json 的 key 承载。

**Architecture:** ConversionService 维护"进行中转换"计数与失败标志；WorkspaceSyncService 推迟同步到转换结束并排除失败条目；WorkspaceFiles 用纯函数清洗标题+去重命名，并把目录↔key 解耦（扫描 item.json 的 key 定位目录）。

**Tech Stack:** Electron 主进程、better-sqlite3、React、vitest。

参考 spec：`docs/superpowers/specs/2026-07-25-sync-flow-design.md`

---

## Task 1: conversion_failed 列 + 类型 + setter

**Files:**
- Modify: `src/main/db/index.ts`（迁移，`if (current < 6)` 之后）
- Modify: `src/shared/types.ts`（`Item`）
- Modify: `src/main/db/items.ts`（本地 `Item` 接口 + 新 setter）

- [ ] **Step 1: 迁移 v7.** 在 `db/index.ts`，`if (current < 6) { ... }` 块之后加：
```ts
  if (current < 7) {
    // Local-only flag: pdf2md conversion failed for this item. Kept out of
    // item.json -- a failed item isn't synced, so collaborators never see it.
    const cols = (db.pragma('table_info(items)') as { name: string }[]).map((c) => c.name)
    if (!cols.includes('conversion_failed')) {
      db.exec(`ALTER TABLE items ADD COLUMN conversion_failed INTEGER NOT NULL DEFAULT 0`)
    }
    db.exec(`INSERT INTO schema_version VALUES (7)`)
  }
```

- [ ] **Step 2: shared Item 类型.** 在 `src/shared/types.ts` 的 `Item` 接口，`added_by: string | null` 之后加：
```ts
  conversion_failed: number
```

- [ ] **Step 3: db 本地 Item 接口 + setter.** 在 `src/main/db/items.ts` 的本地 `Item` 接口，`added_by: string | null` 之后加同样一行 `conversion_failed: number`。然后在文件里（`createItem` 附近）加：
```ts
export function setConversionFailed(itemId: number, failed: boolean): void {
  getDb().prepare('UPDATE items SET conversion_failed = ? WHERE id = ?')
    .run(failed ? 1 : 0, itemId)
}
```

- [ ] **Step 4: typecheck.** `npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c 'error TS'` → 期望 `4`。若因 `Item` 字面量缺字段而升到 5，检查是否有构造 `Item` 的字面量需补 `conversion_failed`（`getAllItemsWithTags` 用 `SELECT *`+spread，不受影响）。`npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS'` → `2`。

- [ ] **Step 5: Commit.**
```bash
cd 'C:\D\Veridian\Veridian 1.0'
git add src/main/db/index.ts src/shared/types.ts src/main/db/items.ts
git commit -m "feat: conversion_failed column + setConversionFailed"
```

---

## Task 2: ConversionService — 计数 + idle hook + 失败/成功置标志

**Files:**
- Modify: `src/main/services/ConversionService.ts`

- [ ] **Step 1: 计数 + hook + 标志.** 在 `ConversionService.ts`：

顶部 import 加：
```ts
import { setConversionFailed } from '../db/items'
```
（`registerAttachment, registerAttachmentDir, listByItem` 等已有 import 保持不变。）

在文件模块作用域（`initConversionService` 之前）加：
```ts
let pendingConversions = 0
let onIdle: (() => void) | null = null

export function hasPendingConversions(): boolean {
  return pendingConversions > 0
}

export function setOnConversionsIdle(fn: () => void): void {
  onIdle = fn
}
```

把 `initConversionService` 里的任务处理器改为：成功清标志、失败置标志、`finally` 递减计数并在归零时触发 idle。即：
```ts
export function initConversionService(): void {
  registerJobType<Pdf2mdPayload>('pdf2md', async (payload, ctx) => {
    const { itemId, pdfPath, outputPath } = payload
    try {
      const mode = getPdf2mdMode()
      const token = getPdf2mdApiToken()

      let mdPath: string
      if (mode === 'precision') {
        if (!token) throw new Error('精准解析模式需要填写 API Token（请前往设置 → PDF 转换）')
        const result = await convertPdfToMarkdownPrecision(pdfPath, token, (p) => {
          ctx.progress(p.message ?? p.state, p.chunk)
        }, outputPath)
        mdPath = result.mdPath
        if (result.imagesDir) {
          grantAccess(result.imagesDir)
          registerAttachmentDir(itemId, result.imagesDir, basename(result.imagesDir))
        }
      }
      else {
        mdPath = await convertPdfToMarkdownAuto(pdfPath, (p) => {
          ctx.progress(p.message ?? p.state, p.chunk)
        }, outputPath)
      }
      grantAccess(mdPath)
      registerAttachment(itemId, mdPath)
      setConversionFailed(itemId, false)   // success clears any prior failure
    } catch (err) {
      setConversionFailed(itemId, true)    // hold this item out of sync
      throw err                            // keep JobQueue's error reporting
    } finally {
      pendingConversions--
      if (pendingConversions === 0) onIdle?.()
    }
  }, { concurrency: 1, maxAttempts: 1 })
}
```

- [ ] **Step 2: enqueue 时计数 +1.** 在 `autoConvertPdfToMd`：只有真正入队那一处 `+1`（"md 已存在直接注册"的早返回**不**计数）。把结尾的 `enqueue(...)` 改为：
```ts
  pendingConversions++
  enqueue<Pdf2mdPayload>('pdf2md', basename(pdfPath), { itemId, pdfPath })
```
在 `manualConvertPdfToMd` 的 `enqueue(...)` 之前同样加 `pendingConversions++`：
```ts
  pendingConversions++
  enqueue<Pdf2mdPayload>('pdf2md', basename(pdfPath), { itemId, pdfPath, outputPath })
  return null
```

- [ ] **Step 3: typecheck + commit.**
```bash
npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c 'error TS'   # expect 4
git add src/main/services/ConversionService.ts
git commit -m "feat: conversion pending-counter + idle hook + failure flag"
```

---

## Task 3: WorkspaceSyncService — 推迟同步 + idle 回调 + 排除失败

**Files:**
- Modify: `src/main/services/WorkspaceSyncService.ts`

- [ ] **Step 1: import.** 顶部加：
```ts
import { hasPendingConversions, setOnConversionsIdle } from './ConversionService'
```

- [ ] **Step 2: 推迟同步.** 把 `scheduleSync` 改为：进行中转换时直接返回（脏条目已在 `dirtyItems` 累积）：
```ts
function scheduleSync(): void {
  // Hold sync while a pdf2md conversion is in flight so the PDF and its
  // converted attachments commit together (one commit, not two). The idle
  // hook re-invokes scheduleSync when conversions settle.
  if (hasPendingConversions()) return
  if (debounceTimer) clearTimeout(debounceTimer)
  debounceTimer = setTimeout(() => {
    debounceTimer = null
    const ctx = getActiveWorkspace()
    if (ctx.kind !== 'github' || !ctx.repoRoot || jobQueued) return
    jobQueued = true
    enqueue<SyncPayload>('workspace.sync', basename(ctx.repoRoot) === 'repo' ? `workspace #${ctx.id}` : basename(ctx.repoRoot), {
      workspaceId: ctx.id!, repoRoot: ctx.repoRoot, pushAfterExport: true,
    })
  }, DEBOUNCE_MS)
}
```

- [ ] **Step 3: idle 回调.** 在 `initWorkspaceSyncService` 内（`registerJobType(...)` 之后、`subscribe(...)` 之前或之后均可）加：
```ts
  // When all in-flight conversions finish, run the sync that was held back.
  setOnConversionsIdle(() => scheduleSync())
```

- [ ] **Step 4: 导出排除失败条目.** 把 `exportAndCommit` 改为过滤掉 `conversion_failed = 1` 的条目：
```ts
async function exportAndCommit(repoRoot: string): Promise<boolean> {
  const db = getDb()
  const failed = new Set(
    (db.prepare('SELECT id FROM items WHERE conversion_failed = 1').all() as Array<{ id: number }>)
      .map((r) => r.id)
  )
  const rawIds = exportAllItems
    ? (db.prepare('SELECT id FROM items').all() as Array<{ id: number }>).map((r) => r.id)
    : [...dirtyItems]
  const ids = rawIds.filter((id) => !failed.has(id))
  dirtyItems = new Set()
  const doCollections = collectionsDirty || exportAllItems
  collectionsDirty = false
  exportAllItems = false

  if (doCollections) exportCollections(db, repoRoot)
  if (ids.length > 0) exportItems(db, repoRoot, ids)
  reconcileDeletions(db, repoRoot)

  return commitAll(repoRoot, `veridian: update ${new Date().toISOString()}`)
}
```

- [ ] **Step 5: typecheck + commit.**
```bash
npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c 'error TS'   # expect 4
git add src/main/services/WorkspaceSyncService.ts
git commit -m "feat: defer sync until conversions settle; exclude failed items from export"
```

---

## Task 4: WorkspaceFiles — 标题目录命名 + 身份解耦（含单测）

**Files:**
- Modify: `src/main/services/WorkspaceFiles.ts`
- Create: `src/main/services/WorkspaceFiles.test.ts`

- [ ] **Step 1: 写纯函数单测.** `src/main/services/WorkspaceFiles.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { sanitizeTitle, uniqueDirName } from './WorkspaceFiles'

describe('sanitizeTitle', () => {
  it('strips Windows-illegal characters', () => {
    expect(sanitizeTitle('a/b:c*d?e"f<g>h|i\\j')).toBe('a b c d e f g h i j')
  })
  it('collapses whitespace and trims', () => {
    expect(sanitizeTitle('  hello    world  ')).toBe('hello world')
  })
  it('trims trailing dots and spaces', () => {
    expect(sanitizeTitle('report...  ')).toBe('report')
  })
  it('falls back to untitled for empty/null', () => {
    expect(sanitizeTitle('')).toBe('untitled')
    expect(sanitizeTitle(null)).toBe('untitled')
    expect(sanitizeTitle('   ')).toBe('untitled')
  })
  it('prefixes Windows reserved names', () => {
    expect(sanitizeTitle('CON')).toBe('_CON')
    expect(sanitizeTitle('lpt1')).toBe('_lpt1')
  })
  it('truncates to 100 chars', () => {
    expect(sanitizeTitle('x'.repeat(200)).length).toBeLessThanOrEqual(100)
  })
})

describe('uniqueDirName', () => {
  it('returns the base when free', () => {
    expect(uniqueDirName('paper', new Set())).toBe('paper')
  })
  it('appends -2 on first collision', () => {
    expect(uniqueDirName('paper', new Set(['paper']))).toBe('paper-2')
  })
  it('skips to the next free suffix', () => {
    expect(uniqueDirName('paper', new Set(['paper', 'paper-2']))).toBe('paper-3')
  })
})
```

- [ ] **Step 2: 运行确认失败.** `npx vitest run src/main/services/WorkspaceFiles.test.ts` — Expected: FAIL（未导出 `sanitizeTitle`/`uniqueDirName`）。

- [ ] **Step 3: 加纯函数 + 扫描辅助.** 在 `WorkspaceFiles.ts`，`const filesDir = ...` 定义之后加：
```ts
const WIN_RESERVED = /^(con|prn|aux|nul|com[1-9]|lpt[1-9])$/i

/** Filesystem-safe folder name derived from an item title. */
export function sanitizeTitle(title: string | null): string {
  let s = (title ?? '').normalize('NFC')
  s = s.replace(/[\\/:*?"<>|]/g, ' ')   // Windows-illegal chars
  s = s.replace(/\p{Cc}/gu, ' ')        // control chars (Unicode Control category)
  s = s.replace(/\s+/g, ' ').trim()
  s = s.replace(/^[.\s]+/, '').replace(/[.\s]+$/, '')
  if (s.length > 100) s = s.slice(0, 100).trim()
  if (!s) return 'untitled'
  if (WIN_RESERVED.test(s)) s = '_' + s
  return s
}

/** `base` if free, else base-2, base-3, ... not in `taken`. */
export function uniqueDirName(base: string, taken: Set<string>): string {
  if (!taken.has(base)) return base
  for (let i = 2; ; i++) {
    const cand = `${base}-${i}`
    if (!taken.has(cand)) return cand
  }
}

/** Map item key -> its current folder name by scanning papers/*/item.json. */
function scanKeyToDir(repoRoot: string): Map<string, string> {
  const map = new Map<string, string>()
  const dir = papersDir(repoRoot)
  if (!existsSync(dir)) return map
  for (const entry of readdirSync(dir)) {
    const jsonPath = join(dir, entry, 'item.json')
    if (!existsSync(jsonPath)) continue
    try {
      const j = JSON.parse(readFileSync(jsonPath, 'utf-8')) as { key?: string }
      if (j.key) map.set(j.key, entry)
    } catch { /* skip unparseable */ }
  }
  return map
}
```

- [ ] **Step 4: 重写 exportItems 使用标题目录.** 把 `exportItems` 函数体开头（`for (const id of itemIds) {` 之前）加目录映射准备，并把循环内 `const dir = itemDir(...)` / `const files = filesDir(...)` 改为按 key 解析的目录。具体：将 `exportItems` 改为：
```ts
export function exportItems(db: Database.Database, repoRoot: string, itemIds: number[]): void {
  const keyToDir = scanKeyToDir(repoRoot)
  const taken = new Set(keyToDir.values())

  for (const id of itemIds) {
    const item = db.prepare('SELECT * FROM items WHERE id = ?').get(id) as
      (Omit<ItemJson, 'creators' | 'tags' | 'collections' | 'attachments'> & { id: number; title: string | null }) | undefined
    if (!item) continue   // deleted since being marked dirty -- reconcileDeletions handles the dir

    // Folder name: reuse the item's existing folder (found by key), else make
    // a fresh sanitized-title folder, deduped against current folder names.
    let dirName = keyToDir.get(item.key)
    if (!dirName) {
      dirName = uniqueDirName(sanitizeTitle(item.title), taken)
      taken.add(dirName)
      keyToDir.set(item.key, dirName)
    }
    const dir = join(papersDir(repoRoot), dirName)
    const files = join(dir, 'files')
    mkdirSync(files, { recursive: true })
```
（该函数**其余部分保持不变**——附件搬运、creators/tags/collections、`const json: ItemJson = {...}`、`writeFileSync(join(dir, 'item.json'), ...)`。因为 `dir`/`files` 现在指向标题目录，其余逻辑自动正确。注意 `item` 的类型断言里补了 `title: string | null` 以便 `sanitizeTitle(item.title)` 通过类型检查。）

- [ ] **Step 5: 重写 importAll 身份逻辑.** 把 `importAll` 里遍历 entries 的循环改为按 `json.key` 认身份，并把文件夹名传给 `importItem`：
```ts
    if (existsSync(dir)) {
      for (const entry of readdirSync(dir)) {
        const jsonPath = join(dir, entry, 'item.json')
        if (!existsSync(jsonPath)) continue
        try {
          const json = JSON.parse(readFileSync(jsonPath, 'utf-8')) as ItemJson
          if (!json.key) continue                 // no identity -> skip
          if (tombstoned.has(json.key)) continue  // locally-deleted, not yet pushed
          importItem(db, repoRoot, json, entry)   // pass the folder name
          treeKeys.add(json.key)
        } catch (err) {
          console.warn(`[WorkspaceFiles] skipping unparseable ${jsonPath}:`, err)
        }
      }
    }
```
（删除原来的 `if (json.key !== entry) json.key = entry` 行。`treeKeys` 现在收集 `json.key`。）

- [ ] **Step 6: importItem 接受文件夹名.** 把 `importItem` 签名与附件目录改为用传入的文件夹名：
函数签名改为：
```ts
function importItem(db: Database.Database, repoRoot: string, json: ItemJson, dirName: string): void {
```
把其中 `const files = filesDir(repoRoot, json.key)` 改为：
```ts
  const files = join(papersDir(repoRoot), dirName, 'files')
```
（其余不变。）

- [ ] **Step 7: reconcileDeletions 按 key 定位目录.** 把 `reconcileDeletions` 的循环改为用 `scanKeyToDir` 定位：
```ts
export function reconcileDeletions(db: Database.Database, repoRoot: string): void {
  const dir = papersDir(repoRoot)
  if (!existsSync(dir)) return
  const tombs = db.prepare("SELECT key FROM tombstones WHERE object_type = 'item'")
    .all() as Array<{ key: string }>
  if (tombs.length === 0) return
  const keyToDir = scanKeyToDir(repoRoot)
  const clear = db.prepare("DELETE FROM tombstones WHERE object_type = 'item' AND key = ?")
  for (const { key } of tombs) {
    const dirName = keyToDir.get(key)
    if (dirName) {
      const target = join(dir, dirName)
      if (existsSync(target)) {
        try { rmSync(target, { recursive: true, force: true }) } catch { /* ignore */ }
      }
    }
    clear.run(key)
  }
}
```

- [ ] **Step 8: exportMissingItems 用 key 映射 + 排除失败.** 改为：
```ts
export function exportMissingItems(db: Database.Database, repoRoot: string): number {
  const keyToDir = scanKeyToDir(repoRoot)
  const rows = db.prepare('SELECT id, key FROM items WHERE conversion_failed = 0')
    .all() as Array<{ id: number; key: string }>
  const missing = rows.filter((r) => !keyToDir.has(r.key))
  if (missing.length > 0) {
    exportCollections(db, repoRoot)
    exportItems(db, repoRoot, missing.map((r) => r.id))
  }
  return missing.length
}
```

- [ ] **Step 9: 运行单测 + typecheck.**
```bash
npx vitest run src/main/services/WorkspaceFiles.test.ts   # expect PASS (9 cases)
npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c 'error TS'   # expect 4
npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -E 'WorkspaceFiles'   # expect empty
```
若 `itemDir`/`filesDir` 因不再被使用而触发 unused 警告，保留它们（其它地方可能引用）或按 lint 结果删除确已无引用者——但先确认 `grep -rn 'itemDir\|filesDir' src/` 的引用情况再决定，避免删掉仍被用的。

- [ ] **Step 10: Commit.**
```bash
git add src/main/services/WorkspaceFiles.ts src/main/services/WorkspaceFiles.test.ts
git commit -m "feat: title-based repo folders; decouple identity from folder name"
```

---

## Task 5: UI — 转换失败红色徽标

**Files:**
- Modify: `src/renderer/src/components/item-tree/ItemListPane.tsx`

- [ ] **Step 1: 徽标 + 行内渲染.** 在 `ItemRow` 组件里，把标题 `<p>...</p>` 之前插入一个失败徽标。找到渲染标题的那段（`{item.title || t('item.untitled')}` 所在的 `<p>`），在包含标题的容器内、标题文本前加：
```tsx
        {item.conversion_failed === 1 && (
          <span
            title={t('item.conversionFailed')}
            style={{
              display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
              width: 16, height: 16, borderRadius: '50%', flexShrink: 0,
              background: '#e5484d', color: '#fff', fontSize: 11, fontWeight: 800,
              marginRight: 6, verticalAlign: 'middle',
            }}
          >
            !
          </span>
        )}
```
放置：把标题 `<p>` 内容改为徽标 + 文本并排。若标题是 `whiteSpace: 'nowrap'` 的单行 `<p>`，最简做法是在该 `<p>` 的最前面插入上面的 `<span>`（inline-flex 徽标可与文本同处一行）。实现者读该 `<p>` 结构后就近插入，保证徽标在标题**前面**。

- [ ] **Step 2: i18n 文案.** 在 `src/renderer/src/i18n/index.ts` 的 `zh` 和 `en` 的 `item` 对象里各加一个键：
zh：`conversionFailed: '转换失败，未同步'`
en：`conversionFailed: 'Conversion failed — not synced'`
（找到 `item: { ... }` 对象，其中已有 `untitled` 等键，按同样格式加。）

- [ ] **Step 3: typecheck web + build.**
```bash
npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS'   # expect 2
npm run build   # expect success
```

- [ ] **Step 4: Commit.**
```bash
git add src/renderer/src/components/item-tree/ItemListPane.tsx src/renderer/src/i18n/index.ts
git commit -m "feat: red conversion-failed badge in item list"
```

---

## Task 6: 全量校验 + 手动验证

- [ ] **Step 1: 全量自动校验.**
```bash
cd 'C:\D\Veridian\Veridian 1.0'
npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -c 'error TS'   # expect 4
npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -c 'error TS'    # expect 2
npx vitest run   # expect all pass (deviceFlow, attribution, WorkspaceFiles) + DB tests skipped
npm run build   # expect success
```

- [ ] **Step 2: 手动 E2E（真实 github 工作空间）.**
1. 导入一篇 PDF（转换开启）→ 观察 GitHub 仓库只多出**一次** commit，`papers/<清洗后的标题>/` 下同时含 pdf + .md + images。
2. 制造转换失败（临时改坏 pdf2md token 或断网）导入一篇 → 该条目**不出现在仓库**，列表该条标题前有红色 ❗；hover 显示"转换失败，未同步"。恢复 token 后手动重转成功 → 红标消失，条目出现在仓库。
3. 导入两篇标题相同的 → 第二篇目录名带 `-2`。
4. 重复导入同一 PDF → 合并进原条目，**不新建目录**。
5. 删除一个已同步条目 → 对应标题目录从仓库移除。

- [ ] **Step 3: 无需额外 commit**（各任务已提交）。发现问题回到对应任务修复。

---

## 完成标准

- 迁移 v7 生效；`WorkspaceFiles.test.ts`（9）通过；块 A/B 测试仍绿。
- 两端 typecheck 回基线（node 4 / web 2）；`npm run build` 成功。
- 导入 PDF 一次提交；失败条目不入仓库且列表标红；目录用标题命名 + `-N` 去重；
  重复 PDF 不建新目录；删除按 key 定位目录。

# AI 检索重排序 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `search_library` 返回给 agent 前,用已配置的聊天模型对 RRF 融合的 top-20 候选做 listwise 重排,取 top-8,失败静默回退到 RRF 序。

**Architecture:** 新增 `src/main/knowledge/rerank.ts`(两个纯函数 + 一个网络包装函数),`hybridSearch` 改为「RRF → 取 top-20 → hydrate 正文 → `rerankHits` → top-8」。检索/向量/RRF/chunker/indexer 全不动。纯函数走 TDD 单测,网络路径靠回退清单 + app 内手动验证。

**Tech Stack:** TypeScript(strict, tabs, 无 `any`)、Vitest、既有 OpenAI 兼容 provider(`chatStream` / `getChatConfig`)。

参见 spec:`docs/superpowers/specs/2026-08-13-ai-rerank-design.md`

---

## File Structure

- **Create** `src/main/knowledge/rerank.ts` — 重排逻辑。导出 `parseRanking`(解析模型输出的 JSON 数组)、`reorderByRanking`(纯重排/回退)、`rerankHits`(调聊天模型,含超时与回退)。
- **Create** `src/main/knowledge/rerank.test.ts` — `parseRanking` 与 `reorderByRanking` 的单测。
- **Modify** `src/main/knowledge/search.ts` — `hybridSearch` 取 top-20 候选并调用 `rerankHits`;新增常量 `RERANK_POOL`。

`SearchHit` 接口(`search.ts` 已定义)不变。`rerank.ts` 只 `import type` 它,避免运行时循环依赖。

---

## Task 1: 纯函数 `parseRanking` + `reorderByRanking`(TDD)

**Files:**
- Create: `src/main/knowledge/rerank.ts`
- Test: `src/main/knowledge/rerank.test.ts`

- [ ] **Step 1: Write the failing test**

写入 `src/main/knowledge/rerank.test.ts`(注意:用 tab 缩进):

```ts
import { describe, it, expect } from 'vitest'
import { parseRanking, reorderByRanking } from './rerank'
import type { SearchHit } from './search'

function hit(chunkId: number): SearchHit {
	return {
		chunkId, itemId: chunkId, itemKey: `K${chunkId}`,
		headingPath: '', seq: chunkId, text: `t${chunkId}`, score: 0,
	}
}

describe('parseRanking', () => {
	it('parses a bare JSON array', () => {
		expect(parseRanking('[2,0,1]')).toEqual([2, 0, 1])
	})
	it('extracts the array from surrounding prose', () => {
		expect(parseRanking('Here you go: [3, 1] done')).toEqual([3, 1])
	})
	it('drops non-integer entries', () => {
		expect(parseRanking('[0, 1.5, "x", 2]')).toEqual([0, 2])
	})
	it('returns [] on garbage', () => {
		expect(parseRanking('no array here')).toEqual([])
		expect(parseRanking('')).toEqual([])
	})
})

describe('reorderByRanking', () => {
	const hits = [hit(10), hit(11), hit(12)]

	it('reorders by the given indices', () => {
		expect(reorderByRanking([2, 0, 1], hits, 3).map((h) => h.chunkId)).toEqual([12, 10, 11])
	})
	it('ignores out-of-range and duplicate indices', () => {
		expect(reorderByRanking([5, 0, 0, 1], hits, 3).map((h) => h.chunkId)).toEqual([10, 11, 12])
	})
	it('backfills uncovered candidates in original order', () => {
		expect(reorderByRanking([1], hits, 3).map((h) => h.chunkId)).toEqual([11, 10, 12])
	})
	it('caps at topK', () => {
		expect(reorderByRanking([2, 0, 1], hits, 2).map((h) => h.chunkId)).toEqual([12, 10])
	})
	it('empty order == fused top-K', () => {
		expect(reorderByRanking([], hits, 2).map((h) => h.chunkId)).toEqual([10, 11])
	})
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/main/knowledge/rerank.test.ts`
Expected: FAIL — `Failed to resolve import "./rerank"` / `parseRanking is not a function`.

- [ ] **Step 3: Write minimal implementation**

写入 `src/main/knowledge/rerank.ts`(tab 缩进):

```ts
import type { SearchHit } from './search'

/** Pull the first JSON integer array out of the model's reply. Tolerates prose
 *  around it; non-integers are dropped. Returns [] when nothing parses. */
export function parseRanking(content: string): number[] {
	const m = content.match(/\[[\s\S]*?\]/)
	if (!m) return []
	let arr: unknown
	try { arr = JSON.parse(m[0]) } catch { return [] }
	if (!Array.isArray(arr)) return []
	return arr.filter((n): n is number => typeof n === 'number' && Number.isInteger(n))
}

/** Reorder `hits` by `order` (indices into hits). Out-of-range/duplicate indices
 *  are skipped; candidates the model never mentioned are appended in their
 *  original (RRF) order. Always returns at most `topK` items and never drops a
 *  candidate that would fit -- so an empty/garbage order degrades to the RRF
 *  top-K, matching pre-rerank behaviour. */
export function reorderByRanking(order: number[], hits: SearchHit[], topK: number): SearchHit[] {
	const seen = new Set<number>()
	const out: SearchHit[] = []
	for (const idx of order) {
		if (idx < 0 || idx >= hits.length || seen.has(idx)) continue
		seen.add(idx)
		out.push(hits[idx])
		if (out.length >= topK) return out
	}
	for (let i = 0; i < hits.length && out.length < topK; i++) {
		if (!seen.has(i)) out.push(hits[i])
	}
	return out
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/main/knowledge/rerank.test.ts`
Expected: PASS (9 tests).

- [ ] **Step 5: Commit**

```bash
git add src/main/knowledge/rerank.ts src/main/knowledge/rerank.test.ts
git commit -m "feat: rerank pure functions (parseRanking, reorderByRanking)"
```

---

## Task 2: `rerankHits` 网络包装(调聊天模型 + 超时 + 回退)

**Files:**
- Modify: `src/main/knowledge/rerank.ts`

网络路径不做单测(依赖 provider);安全性由回退清单保证,最终在 app 内手动验证(Task 4)。

- [ ] **Step 1: Add imports + constants at top of `rerank.ts`**

在 `import type { SearchHit }` 之后加:

```ts
import { getChatConfig, chatStream } from './providers'

const PASSAGE_CHARS = 500
const RERANK_TIMEOUT_MS = 15_000

const RERANK_SYSTEM =
	'You are a search re-ranker. Given a query and a numbered list of passages, ' +
	'order the passages from most to least relevant to the query. ' +
	'Output ONLY a JSON array of the passage numbers, most relevant first, e.g. [3,0,1]. No prose.'
```

- [ ] **Step 2: Append `rerankHits` to `rerank.ts`**

```ts
/** Re-rank RRF candidates with the configured chat model (listwise, single call).
 *  Any failure -- no chat model, offline, timeout, unparseable reply -- silently
 *  falls back to the RRF top-K, so retrieval never breaks. */
export async function rerankHits(query: string, hits: SearchHit[], topK: number): Promise<SearchHit[]> {
	if (hits.length <= topK) return hits.slice(0, topK)
	const cfg = getChatConfig()
	if (!cfg) return hits.slice(0, topK)

	const list = hits
		.map((h, i) => `[${i}] (${h.headingPath || 'text'}) ${h.text.slice(0, PASSAGE_CHARS)}`)
		.join('\n\n')
	const messages = [
		{ role: 'system' as const, content: RERANK_SYSTEM },
		{ role: 'user' as const, content: `Query: ${query}\n\nPassages:\n${list}` },
	]

	const ctrl = new AbortController()
	const timer = setTimeout(() => ctrl.abort(), RERANK_TIMEOUT_MS)
	try {
		const res = await chatStream(cfg, messages, [], () => {}, ctrl.signal)
		return reorderByRanking(parseRanking(res.content), hits, topK)
	} catch {
		return hits.slice(0, topK)
	} finally {
		clearTimeout(timer)
	}
}
```

Note: `chatStream(cfg, messages, tools, onDelta, signal)` 来自 `providers.ts`;`tools` 传 `[]`,`onDelta` 传 no-op,取返回值 `res.content`。`getChatConfig()` 返回 `null` 表示未配置。

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: 无输出(通过)。若报 `ChatMessage` 类型不匹配,把 `messages` 显式标注为 `import('./providers').ChatMessage[]`。

- [ ] **Step 4: Run existing tests still green**

Run: `npx vitest run src/main/knowledge/rerank.test.ts`
Expected: PASS(纯函数不受影响)。

- [ ] **Step 5: Commit**

```bash
git add src/main/knowledge/rerank.ts
git commit -m "feat: rerankHits -- LLM listwise rerank with timeout and RRF fallback"
```

---

## Task 3: 接入 `hybridSearch`

**Files:**
- Modify: `src/main/knowledge/search.ts`

- [ ] **Step 1: Add import + constant**

在 `search.ts` 顶部 import 区加(紧跟现有 provider import 之后):

```ts
import { rerankHits } from './rerank'
```

在 `const CANDIDATES = 30` / `const RRF_K = 60` 旁加:

```ts
const RERANK_POOL = 20   // fused candidates handed to the reranker; it returns topK
```

- [ ] **Step 2: Replace the body of `hybridSearch`**

把现有 `hybridSearch`(从 `const fused = rrfFuse(...)` 到 `return top.flatMap(...)`)替换为:

```ts
	const fused = rrfFuse([ftsIds, vecIds])
	// Over-select a rerank pool; the reranker narrows it to topK. If reranking is
	// unavailable it falls back to this RRF order, so behaviour degrades safely.
	const pool = [...fused.entries()].sort((a, b) => b[1] - a[1]).slice(0, RERANK_POOL)
	if (!pool.length) return []

	const kdb = getKnowledgeDb()
	const rows = kdb.prepare(
		`SELECT id, item_id, item_key, heading_path, seq, text FROM chunks WHERE id IN (${pool.map(([id]) => id).join(',')})`
	).all() as { id: number; item_id: number; item_key: string; heading_path: string; seq: number; text: string }[]
	const byId = new Map(rows.map((r) => [r.id, r]))
	const candidates = pool.flatMap(([id, score]) => {
		const r = byId.get(id)
		return r ? [{
			chunkId: r.id, itemId: r.item_id, itemKey: r.item_key,
			headingPath: r.heading_path, seq: r.seq, text: r.text, score,
		}] : []
	})
	return rerankHits(query, candidates, topK)
```

(注:candidates 按 `pool`(已按 RRF 分排序)顺序构建,所以回退时就是原 RRF top-K。`topK` 默认仍是 8。)

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`
Expected: 无输出(通过)。

- [ ] **Step 4: Full unit test run**

Run: `npm test`
Expected: 全绿(含既有 `search.test.ts` 的 `rrfFuse`/`toFtsQuery` 与新 `rerank.test.ts`)。

- [ ] **Step 5: Commit**

```bash
git add src/main/knowledge/search.ts
git commit -m "feat: wire LLM reranking into hybridSearch (top-20 pool -> top-8)"
```

---

## Task 4: App 内手动验证

**Files:** 无(验证)。

- [ ] **Step 1: 确认聊天模型已配置**

在 app 设置里确认已配置聊天 provider(DeepSeek/Kimi/Claude 等),否则重排会走"未配置→回退"路径,无法观察效果。

- [ ] **Step 2: 若 dev server 未开则启动**

先问用户是否已开着 app(见记忆:用户常已运行真实 app,避免起第二个实例)。未开时:
Run: `npm run dev`

- [ ] **Step 3: 触发一次检索**

在 AI 助手里就某篇已转 markdown 的文献提一个具体问题,观察:
- 正常返回答案与引用(说明重排未破坏检索)。
- 主进程日志无未捕获异常。

- [ ] **Step 4: 验证回退**

临时断网(或在设置里清空聊天 key)后再问一次:应仍能基于 FTS/向量返回结果(走回退),不报错、不卡死(≤15s 超时)。恢复配置。

- [ ] **Step 5: 无需提交**

验证任务无代码改动。

---

## Self-Review

- **Spec 覆盖**:打分器=复用聊天模型(Task 2 `getChatConfig`+`chatStream`)✅;listwise 单次调用(Task 2 单条 messages)✅;top-20(Task 3 `RERANK_POOL`)✅;始终开、无设置(全程未加 setting)✅;500 字截断(Task 2 `PASSAGE_CHARS`)✅;回退清单 5 条(未配置/失败/超时/解析失败/候选不足——Task 2 `rerankHits` 的 guard + try/catch + Task 1 `reorderByRanking`)✅;`reorderByRanking` 单测 4 类场景(Task 1)✅。
- **占位符扫描**:无 TBD/TODO,所有代码步骤含完整代码。
- **类型一致性**:`parseRanking`/`reorderByRanking`/`rerankHits` 三处签名在 Task 1–3 一致;`SearchHit` 字段与 `search.ts` 现有定义一致;`chatStream` 五参调用与 `providers.ts` 签名一致。
